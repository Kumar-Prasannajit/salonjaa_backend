# Progress

Status as of last session. Update this file whenever a module ships or a decision is made — this is the single source of truth for "what actually exists right now," not the TRD (which describes the full target system, not current state).

## Done and tested (real Postgres + Redis, via Postman)

### Module 1 — Foundation + Auth
- `POST /api/v1/auth/send-otp` — `{ email }` → `{ success, message }`
- `POST /api/v1/auth/verify-otp` — `{ email, otp }` → `{ success, accessToken, refreshToken, user }`. First verify for an email auto-creates the user as role `CUSTOMER`.
- `POST /api/v1/auth/refresh-token` — `{ refreshToken }` → `{ accessToken }` (no `success` key — non-standard shape, intentional, matches frontend_handover.md exactly)
- `POST /api/v1/auth/logout` — auth required, `{ refreshToken }` → `{ success }`
- Migration `0000_shocking_maginty.sql` also created `notifications` and `notification_templates` (TRD §11 Notification Design) and `audit_logs` (TRD §12 Security Design). `notifications` is actively used today — the OTP provider inserts a row per send and updates it to `SENT`/`FAILED` for delivery tracking. `notification_templates` and `audit_logs` are schema only, no consumers yet: `notification_templates` is reserved for the Notification module (Development Order step 8), `audit_logs` for audit logging (Development Order steps 1 and 9).

### Module 2 — User + Address
- `GET /api/v1/users/me` → `{ id, name, email, phone, gender, dob, profileImage }` — **field is `name`, not `fullName`**, per frontend_handover.md's documented contract (differs from the PATCH request body field below — intentional, not a bug)
- `PATCH /api/v1/users/me` — `{ fullName?, gender?, dob? }` → `{ success, data }`
- `GET/POST /api/v1/users/me/addresses`, `PATCH/DELETE /api/v1/users/me/addresses/:id` — full CRUD, ownership-scoped, `isDefault` auto-clears other addresses
- **Deferred:** `GET /users/me/bookings` — depends on the `bookings` table, which belongs to the Booking module (not built). Don't stub this early; wire it up when Booking ships.

### Module 3 — Salon + Branch
- Migration `0002_lush_frightful_four.sql` (salon_owner_profiles, salons, branches, branch_holidays, branch_capacity_rules)
- `POST/GET /api/v1/salons`, `GET/PATCH/DELETE /api/v1/salons/:salonId` — all require `SALON_OWNER` role
- `POST /api/v1/branches` (needs `salonId`), `GET/PATCH /api/v1/branches/:id`, `GET /api/v1/branches?salonId=`
- `POST /api/v1/branches/:id/holidays`, `DELETE /api/v1/branches/:id/holidays/:holidayId`
- `POST /api/v1/branches/:id/capacity-rule`
- **Deferred (real TRD tables, but no documented endpoint yet — don't build these until a contract exists):** `salon_ownership_history`, `salon_gallery_images`, `branch_slot_templates`, `calendar_events`
- First salon creation by a user auto-creates their `salon_owner_profiles` row (find-or-create pattern) — there is no separate "become an owner" endpoint anywhere in the docs

### Module 4 — Catalogue + Staff
- Migration `0003_clever_living_tribunal.sql` (staff, staff_leaves, branch_services, service_categories, staff_services)
- `POST/GET /api/v1/staff`, `GET/PATCH/DELETE /api/v1/staff/:id` — all require `SALON_OWNER` role + branch ownership (`BranchService.assertOwned`, now public for cross-module use — see Branch's Staff/Service note below). `GET /staff` takes optional `branchId` query; omitted lists staff across every branch the caller owns (same pattern as `GET /branches`'s optional `salonId`). `DELETE` soft-deletes (`deletedAt`).
- `POST /api/v1/staff/:id/leave`, `DELETE /api/v1/staff/:id/leave/:leaveId` — leave cancellation sets `status = CANCELLED` per TRD ("cancellation changes status, never removes record") — `staff_leaves` has no `deletedAt` column, this is intentional, not a missed soft-delete.
- `POST/GET /api/v1/services`, `GET/PATCH/DELETE /api/v1/services/:id` — same branch-ownership + optional-`branchId` pattern as Staff. Table is `branch_services` in the DB (TRD naming), exposed as `/services` per frontend_handover.md.
- `POST /api/v1/services/:id/staff`, `DELETE /api/v1/services/:id/staff/:staffId` — staff↔service assignment; 409 on duplicate assignment, 404 if the staff member isn't in the same branch as the service (existence not leaked across branches, same philosophy as `assertOwned`).
- **`service_categories` has no CRUD endpoint anywhere in the docs** (TRD calls it "platform category lifecycle"; the Admin API inventory is empty in frontend_handover.md). Rows come only from `npm run db:seed` (8 starter categories) — same precedent as `db:grant-role` for roles. Build a real Admin endpoint only when that module defines the contract; don't add owner-facing category CRUD speculatively.
- **Deferred (real TRD table, no documented endpoint):** `service_images` (multi-image gallery per service). `branch_services` does have a single `imageUrl` column (TRD's "image URL"), settable via `PATCH /services/:id` — same precedent as `salons.logo`/`coverImage` being PATCH-only, undocumented-in-create fields.
- Money/experience fields (`basePrice`, `salary`, `consultationFee`) use `doublePrecision`, not `numeric` — the installed `drizzle-orm@0.33` doesn't support `numeric(..., { mode: "number" })`, and this codebase has no other numeric-string handling yet. Matches the existing `latitude`/`longitude` precedent. Revisit if exact decimal precision becomes a requirement (e.g. for Payment).
- `BranchService.assertOwned` was made `public` (was `private`) so Staff and Service can call it directly, per CONVENTIONS.md's "call the parent module's service to verify ownership" rule — same relationship Branch already has with `SalonService.assertOwned`.

### Module 5 — Availability
- Migration `0004_kind_mattie_franklin.sql` adds a **minimal** `bookings` table — only `branchId`, `selectedStaffId`, `scheduledStart`, `scheduledEnd`, `bookingStatus`, timestamps. This is deliberately not the full TRD `bookings` schema; Booking (Module 6, not started) will `ALTER TABLE` to add `customerId`, `salonId`, `bookingNumber`, `bookingType`, amounts, reasons, and approval/completion/cancellation timestamps when it builds real create/approve/cancel/reschedule flows. Never soft-deleted or hard-deleted, per TRD.
- `GET /api/v1/availability/slots`, `GET /api/v1/availability/staff` — both public, no auth. Query: `branchId`, `date` (YYYY-MM-DD, not in the past), `serviceIds` (**comma-separated** — not specified in frontend_handover.md, this is the chosen convention; e.g. `?serviceIds=id1,id2`).
- Three architectural gaps existed before this module could be built at all — resolved with the user, not guessed:
  1. **No slot-template CRUD exists** (`branch_slot_templates` was explicitly deferred in Module 3, no endpoint anywhere). Resolved: slots are generated at a **fixed 30-minute interval** (`DEFAULT_SLOT_INTERVAL_MINUTES` in `shared/constants.ts`) across the branch's `openingTime`→`closingTime`, ignoring `branch_slot_templates` entirely. Revisit if/when a template contract is ever added.
  2. **Capacity validation needs live bookings, but Booking (Module 6) doesn't exist.** Resolved: the minimal `bookings` table above. Right now it's always empty (nothing can insert into it until Module 6 ships), so capacity is effectively governed by chairs/staff only — but the overlap-counting logic is real and already correct for when rows start appearing.
  3. **Redis caching skipped.** TRD lists a `cache:availability:{...}` key but explicitly leaves its TTL "not numerically finalized" — decided to compute live on every request for MVP rather than wire invalidation across 5 mutation points (staff/leave/holiday/capacity-rule/service) for an unfinalized optimization. Add later as a hardening pass if needed.
- Capacity formula implemented exactly per TRD: `MIN(branch.totalChairs, activeServiceStaffCount, capacityOverride)`, where `activeServiceStaffCount` excludes staff with an `APPROVED` leave overlapping the specific candidate slot window (not just the whole day). Overlap check for both staff leave and existing bookings uses the TRD's exact `existing.start < new.end AND existing.end > new.start` rule (`end == start` is correctly treated as non-overlapping).
- `GET /availability/staff` is a **day-level** listing (frontend_handover.md's contract has no time param, only `date`): it excludes a staff member only if their `APPROVED` leave fully covers the entire business-hours window for that date. A staff member on leave for only part of the day still appears — the precise per-slot conflict is re-checked by `GET /availability/slots` and again inside the future booking-creation transaction (Module 6).
- `slotId` in the response is a plain `"HH:MM-HH:MM"` string (e.g. `"10:00-11:30"`) — not a persisted row, per TRD ("slot IDs are UI helpers"). Module 6's `POST /bookings` will need to decode this same format when it accepts a `slotId`.
- Response shape is a **bare array** for both endpoints (no `{success,data}` envelope), matching frontend_handover.md's literal documented shape — same exception category as `GET /users/me`.
- Verified end-to-end manually against a temporary local instance (real salon/branch/service/staff via the actual API, salon verification flipped directly in Postgres since no Admin endpoint exists yet) before this entry was written: confirmed capacity math, staff-leave capacity reduction, holiday short-circuit (`[]`), invalid-service 400, and the overlap boundary edge case, then cleaned up all test rows. Postman collection/spec ("Salonjaa API") has **not** been updated yet for this module — still only covers Modules 1-4.

### Module 6 — Booking
- Migration `0005_boring_timeslip.sql` ALTERs the minimal `bookings` table from Module 5 up to (most of) the TRD's full schema, and adds `booking_services`, `booking_status_history`, `booking_reschedule_requests`. Two intentional deviations from the TRD's exact column list, both decided with the user:
  - `bookings.customerName`/`bookings.customerPhone` (nullable) — TRD's bookings table has no columns for an unregistered walk-in customer's identity, but `POST /salon-bookings/walk-in`'s documented body requires them. `customerId` stays null for walk-ins rather than fabricating a synthetic `users` row.
  - `booking_reschedule_requests.responseReason` (nullable) — TRD lists a single `reason` column, but `POST /bookings/:id/reject-reschedule`'s documented body needs its own reason distinct from the original requester's.
- `POST /api/v1/bookings` (Customer) — creates a PENDING booking, snapshots each service's name/duration/price into `booking_services` (immutable), reserves capacity immediately. Bare `{ bookingId, status }` response, no envelope, per frontend_handover.md.
- `GET /api/v1/bookings/:id` (customer owner, owning Salon Owner, or ADMIN role) — bare `{ booking: {...} }`, per frontend_handover.md. `GET /api/v1/bookings/my-bookings` (Customer) — default envelope, optional `status` filter.
- `POST /api/v1/bookings/:id/cancel` (Customer owner) — **provisional policy, explicitly flagged**: any PENDING/APPROVED booking cancellable any time before scheduled_start, no cutoff, no strike ever recorded. The client's actual cancellation/strike policy is still unresolved (context.md Pending Decisions); user explicitly asked to ship this safe placeholder now and update later.
- `POST /api/v1/bookings/:id/reschedule-request`, `/approve-reschedule`, `/reject-reschedule` — customer proposes a new time, owning Salon Owner decides. No `requestId` in these routes (matches the documented paths exactly), so approve/reject always resolve the booking's latest PENDING `booking_reschedule_requests` row. Full capacity/staff recheck happens at approve time; only a light branch-hours/holiday check happens at request time.
- `GET /api/v1/salon-bookings`, `/approve`, `/reject`, `/propose-reschedule`, `/walk-in` (owning Salon Owner) — `propose-reschedule` creates a `SALON`-initiated reschedule request, but **no documented endpoint exists yet for the customer to accept/reject it** (context.md Pending Decisions flags this exact gap) — the request sits PENDING until such an endpoint is added. `walk-in` requires `staffId` (derives branchId/salonId from the staff record — decided with the user, since the documented walk-in body has no branchId/salonId field at all) and creates the booking straight to `APPROVED` (no customer-facing approval flow needed for walk-ins, per TRD). `POST /salon-bookings/:id/block-slot` is explicitly "Future Feature — Not MVP" per the docs — not implemented.
- **Not built**: booking check-in OTP (`booking_otps` table, one-time check-in code) — no endpoint for this exists anywhere in frontend_handover.md, so per scope discipline it's skipped entirely, not just deferred. Revisit only if a contract is added. The "approve then 15-minute payment window" business rule the client described is also not enforced here — `BookingStatus` has no intermediate "awaiting payment" state, and there's no Payment module yet to actually check payment status against; decided with the user that Payment module will own this rule when it's built (creating the order on approval and enforcing its own timeout), rather than Booking carrying a rule it can't act on.
- Concurrency: `POST /bookings` and walk-in creation run inside one Postgres transaction serialized per-branch via `pg_advisory_xact_lock(hashtext(branchId))` — no Redis lock involved (decided with the user; matches "Redis is never the booking source of truth"). Verified manually: two concurrent-ish overlapping bookings for different staff both succeed up to capacity, a third correctly gets `409`, and requesting the same staff member twice for an overlapping window correctly gets its own `409` ("staff no longer available") independent of the branch-capacity check.
- Booking expiry: BullMQ job (`booking.expire` queue) scheduled on every PENDING booking creation, delay = `BOOKING_DEFAULT_EXPIRY_HOURS` (env, default 12). Worker re-checks the booking is still PENDING before acting (safe no-op otherwise) rather than tracking/cancelling stale jobs — verified both paths manually (a still-PENDING booking flips to EXPIRED with a status-history row; an already-CANCELLED booking is untouched).
- **Bug found and fixed along the way**: `src/config/queue.ts`'s pre-existing `QUEUE_NAMES` (from earlier scaffolding, not written in this module) used `:` as a separator (e.g. `"booking:expire"`) — BullMQ v5 rejects any queue name containing `:` and throws at construction time. Renamed all of them to use `.` instead. This crashed the locally-running dev server via `tsx watch`'s auto-reload the moment `booking-expiry.queue.ts` was added, before the fix landed — server was restarted afterward and confirmed clean.
- Verified end-to-end manually against a temporary local instance (real salon/branch/2 staff/service, salon verification flipped directly in Postgres): full create→approve/reject→cancel→reschedule-request→approve-reschedule flow, walk-in, capacity exhaustion (2 chairs/2 staff → 3rd overlapping booking rejected), staff-specific conflict, holiday blocking, operating-hours validation, salonId/branchId mismatch validation, role-based 403s, and the expiry worker (both the real transition and the safe no-op) — all confirmed correct, then all test rows cleaned up. Postman collection/spec still not updated (unchanged since Module 4, per the user's earlier explicit "no need" — see the /postman:sync note in git history).

### Module 7 — Payment + Coupon
- Migration `0006_chunky_pixie.sql` adds `payments`, `payment_transactions`, `refunds`, `refund_history`, `coupons`, `coupon_usages`, `booking_coupons`, `settlements`, `settlement_bookings` per TRD §4, unchanged from the TRD's column lists.
- Provider: Razorpay (resolves context.md's previously-pending "exact payment-provider selection"). `RAZORPAY_KEY_ID`/`RAZORPAY_KEY_SECRET` in `.env` — app boots fine without them (same pattern as SMTP); `POST /payments/create-order` and `POST /payments/verify` throw a clear `InternalServerError` at request time if unset, not a boot-time crash. `PaymentProvider` interface + `RazorpayProvider` implementation in `src/providers/payment/`, matching the existing OTP/Email provider-adapter pattern.
- **Scope decision**: only the two documented client-driven endpoints are built (`create-order` → Razorpay checkout on the frontend → `verify` with the returned `orderId`/`paymentId`/`signature`). No webhook receiver endpoint exists anywhere in frontend_handover.md, so none was built — payment status only updates when the client actually calls `/verify`. Signature verification is manual HMAC-SHA256 (`orderId|paymentId`, key_secret) via `crypto.timingSafeEqual`, matching Razorpay's documented client-side verification scheme.
- `POST /payments/create-order` (Customer) requires the booking to be `APPROVED` first — decided with the user ("if salon owner approves then customer will pay") — and blocks a second order while one is `PENDING`/`SUCCESS` (a `FAILED` order can be retried). Literal bare `{ orderId, amount, currency }` response. `POST /payments/verify` is idempotent (re-verifying an already-`SUCCESS` payment just returns success again) and literal bare `{ success, paymentStatus }`.
- `GET /payments/:paymentId` (payment's own customer, owning Salon Owner via the booking's salonId, or ADMIN), `GET /payments/my-payments` (Customer) — default envelope.
- `POST /payments/refund-request` (Customer) — **provisional policy, explicitly flagged**, same treatment and same underlying pending-decision entry as booking cancellation: booking must be `CANCELLED` or `COMPLETED` with a `SUCCESS` payment, full amount only, one refund per payment, no time-window/percentage rule since none is defined. User explicitly asked to ship this now and add the real policy later (alongside the cancellation policy).
- `GET /payments/refunds` (Customer), `GET /payments/salon-settlements` (owning Salon Owner) — default envelope; settlements is a pure read against whatever rows exist (TRD: "manual MVP records", nothing creates them yet — same precedent as salon verification being flipped manually in Postgres).
- `POST /payments/coupons/validate` — pure preview check, **no side effects**: no documented endpoint anywhere attaches a coupon to a booking (`POST /bookings`'s body has no `couponCode` field), so `usedCount`/`coupon_usages`/`booking_coupons` are never written. Literal bare `{ valid, discount }` on success; invalid/expired/exhausted/below-minimum coupons throw `422` (matches the endpoint's documented error codes) rather than a soft `{valid:false}`.
- Coupons have no CRUD endpoint anywhere (same situation as `service_categories` in Module 4) — `npm run db:seed` now also seeds 2 starter coupons (`WELCOME10` 10%/max ₹150/min ₹200, `FLAT50` fixed ₹50/min ₹300), decided with the user, same precedent as category seeding.
- **Not enforced**: the client's described "approve then 15-minute payment window" — no `AWAITING_PAYMENT` state exists in `BookingStatus`, and there's no reliable way to check payment completion without a webhook. Decided with the user in Module 6 that Payment module would own this rule; still not implemented — would need either a webhook (undocumented, skipped per scope discipline) or client-side polling, neither of which is currently in the contract.
- Verified end-to-end against a temporary local instance, including **real Razorpay test-mode API calls** (not mocked) once the user supplied real test keys: create-order gating (rejects a PENDING booking, succeeds once APPROVED, blocks a duplicate while PENDING/SUCCESS, allows retry after FAILED), signature verification both ways (a fabricated signature correctly fails and marks the payment FAILED; a correctly-computed HMAC using the real key_secret correctly succeeds and is idempotent on re-verify), refund-request gating (blocked before cancellation, blocked without a SUCCESS payment, blocked on duplicate), ownership checks on payment detail, empty settlements read, and coupon math (percentage, fixed, max-discount cap, below-minimum 422, unknown-code 422) — all confirmed correct, then all test rows cleaned up (seeded coupons left in place).

### Module 8 — Review + Notification
- **Notification**: `NotificationChannelProvider` interface in `src/providers/notification/`, matching the existing OTP/Email/Payment adapter pattern the user asked to keep. `EmailChannelProvider` (the only implementation) wraps the existing `EmailProvider`. Adding SMS/WhatsApp later is a one-line registration in `src/providers/notification/index.ts` — the dispatch worker already looks providers up generically by `notifications.type` and treats a missing entry as "not deliverable yet," not a crash. No new endpoints — `frontend_handover.md` has no Notification section at all; this is a purely internal delivery mechanism, matching TRD §11's description of it as event-triggered email, not a queryable resource.
- Delivery is async via BullMQ (`notification.send` queue, TRD §10): `NotificationService.notify()` persists a `notifications` row then enqueues delivery — it never throws, so a notification failure can never break the caller's primary action (booking approval, cancellation, etc.), same defensive philosophy as `AuthService.sendOtp`'s existing try/catch. Worker retries 3x with exponential backoff, marks `FAILED` only after the final attempt.
- Templates are hardcoded per event type (`notification.templates.ts`), not read from the `notification_templates` table — matches the existing precedent (`EmailOtpProvider` already hardcodes its OTP subject/body; the table has stayed schema-only/no-consumer since Module 1).
- Wired into Booking (created/approved/rejected/expired/completed/cancelled/all 3 reschedule directions) and Payment (refund requested) at the exact TRD §11 event list, minus OTP (already has its own path) and promotions (module not built). Notifications route to whichever party makes sense per event (customer for their own booking's status changes, the salon owner for customer-initiated actions like cancellation/reschedule-request) — added `SalonService.findOwnerUserId(salonId)` for the latter. Walk-in bookings are skipped (no `customerId`, nothing to notify).
- **Real gap resolved with the user**: Reviews require a `COMPLETED` booking, but no documented endpoint anywhere transitions a booking to that status. Added a new BullMQ job (`booking.complete`, not in the TRD's original list) that auto-transitions an `APPROVED` booking to `COMPLETED` when `scheduledEnd` passes — same delayed-job mechanism as booking expiry. Rescheduling a booking correctly re-times (not duplicates) this job via a deterministic `jobId` (`rescheduleBookingCompletion`), so an approved reschedule doesn't leave a stale job that would complete the booking at the *old* time.
- **Review**: `POST /reviews`, `PATCH /reviews/:reviewId`, `GET /reviews/:reviewId` (public), `GET /reviews/salon/:salonId` / `.../service/:serviceId` / `.../staff/:staffId` (public), `POST /reviews/:reviewId/report`, `POST /reviews/:reviewId/reply` (owning Salon Owner). `reviews` has no direct `serviceId` column (matches TRD §4 exactly) — the service-scoped listing is a join through `bookings` → `booking_services`. Category ratings (`serviceRating`/`staffRating`/`hygieneRating`/`ambienceRating`/`productRating`) live in `review_category_ratings` as generic `(category, rating)` rows per TRD's schema; added a `review_category` enum (`SERVICE`/`STAFF`/`HYGIENE`/`AMBIENCE`/`PRODUCT`) since the TRD's enum list (§6) doesn't define one but the documented request body needs concrete values.
- **Not built**: `POST /reviews/:reviewId/images` — frontend_handover.md says "image upload contract not supplied," genuinely no format to build against. Decided with the user to skip entirely rather than guess (no multipart/base64/URL assumption baked in); the `review_images` table itself also wasn't created, consistent with not building schema ahead of a real consumer.
- **Provisional, same treatment as booking cancellation/refund**: the review edit period ("allowed edit period" per frontend_handover.md) is one of context.md's Pending Decisions — no cutoff is enforced; `PATCH /reviews/:reviewId` works at any time and just sets `isEdited = true`.
- Verified end-to-end against a temporary local instance, **with real SMTP delivery** (Mailtrap sandbox, already configured in `.env`) — not mocked: booking creation → notification persisted → BullMQ job → actual email sent → `notifications.status` flips `PENDING → SENT`, confirmed for `BOOKING_CREATED`, `BOOKING_APPROVED`, and `BOOKING_COMPLETED`. Forced the auto-completion job to a short delay to confirm the real transition (`APPROVED → COMPLETED`, `completedAt` set) rather than just reading the code. Then confirmed the full Review flow: create on a genuinely-completed booking, blocked on a still-`PENDING` one (409), duplicate-review rejection, rating-bounds validation (400), edit setting `isEdited`, public unauthenticated reads (detail + all three listing endpoints, including the service-join listing), owner reply embedding correctly in the review DTO, report + duplicate-report rejection (409), and the reply-requires-`SALON_OWNER` role check (403 for a customer). All test rows cleaned up afterward.

### Module 9 — Admin (first slice: salon approval)
- **Not from a spec** — unlike every prior module, `frontend_handover.md`'s Admin section is explicitly empty ("no endpoints supplied... do not implement until its endpoint inventory is defined") and context.md lists the Admin API inventory as a Pending Decision. The user explicitly asked to co-define the contract live rather than wait for one, and picked salon approval as the first slice since it's the single most-hit manual workaround (every test salon needed a direct `UPDATE salons SET verification_status='VERIFIED'` via psql up to this point).
- Migration `0008_chemical_maddog.sql` adds `admin_actions` (TRD §4's admin-specific action log). No other new tables — everything else this slice needs already existed.
- `GET /api/v1/admin/salons?status=PENDING|VERIFIED|REJECTED`, `GET /api/v1/admin/salons/:salonId`, `POST .../verify`, `POST .../reject` (`{ reason }` required), `POST .../suspend` (`{ reason }` required, sets `salons.status = SUSPENDED`), `POST .../reactivate` (back to `ACTIVE`) — all `requireRole(ADMIN)`, same auth model as everywhere else (`npm run db:grant-role -- <email> ADMIN`, no separate portal/2FA — nothing in the docs calls for one).
- **`audit_logs` finally has a real writer** — that table existed since Module 1 with zero consumers (see PROGRESS.md's old Module 1 note). Every one of these 4 actions writes both an `audit_logs` row (full actor/entity/old-value/new-value/IP/user-agent per TRD §12's security design) and an `admin_actions` row (the simpler "which admin did what" log TRD §4 separately calls for). Verified both are populated correctly for every action, including the old→new value diff.
- Notifications added for the 4 salon lifecycle events (`SALON_VERIFIED`/`REJECTED`/`SUSPENDED`/`REACTIVATED`), routed to the owner via `SalonService.findOwnerUserId`. Not in the TRD's literal Notification event list, but a natural, low-risk extension of the pattern established in Module 8 (every other state transition already notifies the affected party) — flagged here rather than silently added.
- Verified end-to-end against a temporary local instance: full list→detail→verify flow with real audit trail confirmed via SQL, suspend/reactivate cycle, reject-with-reason (and validation-rejects reject/suspend without a reason), status-filtered listing, non-admin role correctly gets `403`, and the `SALON_VERIFIED` notification confirmed sent via real SMTP. All test rows cleaned up afterward.
- **What Admin still doesn't cover** (deferred, not started): refund approval/processing (refunds still get created `PENDING` and go nowhere), settlement creation (`GET /payments/salon-settlements` still always returns `[]`), review report resolution, coupon/category CRUD (still `db:seed`-only), complaints and customer strikes (their TRD tables don't exist yet), reports/analytics. Same pattern as this slice — pick the next priority and define its contract together before building.

### Module 9b — Admin: Refunds
- New contract this time: `docs/ADMIN_CONTRACT.md`, written by the user after seeing the salon-approval implementation, covering Refunds/Complaints/Reports in priority order. Explicitly told to reconcile its audit_logs+admin_actions guidance with what salon-approval actually built rather than treat the doc as authoritative on that pattern — it wasn't, the two lined up exactly, no reconciliation needed.
- `GET /api/v1/admin/refunds` (optional `status` filter), `GET /api/v1/admin/refunds/:id` — join refund+booking+payment+customer into one response so the admin has full context without a second lookup (per the contract). `POST .../approve` (`{ notes? }`) and `POST .../reject` (`{ reason }` required) — both restricted to `PENDING` refunds only (409 otherwise), both write `audit_logs` + `admin_actions` (same pattern as salon moderation) *and* `refund_history` (Module 7's existing table — this is its first update ever; it previously only got written once, at refund-request creation).
- New files under `src/modules/admin/` prefixed `admin-refund.*` rather than folded into the existing `admin.*` files — Admin is clearly going to keep growing (Complaints and Reports next in this same contract), and a single admin.service.ts covering unrelated resources (salons, refunds, complaints, reports share no business logic with each other) would become an unmanageable god-class. Existing salon-admin files were left untouched/unrenamed to avoid churning tested code — only new resources get the `admin-<resource>.*` naming going forward.
- **No automated eligibility logic, by design** — the contract is explicit: `context.md`'s cancellation/refund policy is still an open Pending Decision, so every refund is read and decided by hand. "Approve" only marks the decision (`approvedBy` set) — no money actually moves, since MVP payment is pay-at-salon-style with no gateway wired for issuing funds yet.
- Added `REFUND_APPROVED`/`REFUND_REJECTED` notification events, routed to the refund's customer — not in the contract's literal scope, but a direct extension of the pattern every other Admin/Booking/Payment decision already uses (flagging, not hiding, the addition).
- Verified end-to-end against a temporary local instance, including a **second real Razorpay payment** (paid via correctly-computed HMAC signature, same as Module 7's test): full list (with joined context) → approve flow confirmed via direct SQL across all three logs (`audit_logs` old→new diff, `admin_actions`, `refund_history`'s new second row), idempotency guard (approving twice → 409), reject-without-reason (400) and reject-with-reason on a second refund, non-admin role → 403, and the `REFUND_APPROVED` notification confirmed sent via real SMTP. Also dogfooded the Module 9a salon-verify endpoint itself to set up this test's salon, rather than the old manual SQL flip. All test rows cleaned up afterward.

### Module 9c — Admin: Complaints
- New table `complaints` (migration `0009_cheerful_stepford_cuckoos.sql`). One deliberate deviation from TRD §4: the contract (`docs/ADMIN_CONTRACT.md` §2) gives `POST /complaints` a single generic `referenceId` field, not TRD's implied per-entity FK split (nullable booking/salon IDs) — followed the documented request body exactly. Column is `filedByUserId`, not `customerId`, since the contract has both Customer *and* Salon Owner filing.
- **Filing side lives in a new, small `src/modules/complaint/` module** (`POST /complaints` only) — reasoning given to the user before building: a complaint isn't owned by any existing module (its `type` can point at a booking, payment, salon, staff, refund, or nothing), and it's filed by either role, so it doesn't fit Booking's or Payment's mental model any more than the other. Same reasoning that already put Review in its own module despite being booking-scoped.
- **Resolution side** (`GET/POST /admin/complaints/...`) follows the Module 9b pattern exactly: new `admin-complaint.*` files in `src/modules/admin/`, own repository querying `complaints` directly rather than reusing `ComplaintRepository` (mirrors `admin-refund.*` not reusing `PaymentRepository`).
- `POST /admin/complaints/:id/resolve` (`{ resolutionNotes }` required) and `.../reject` (`{ reason }` required) both only act on `OPEN`/`IN_PROGRESS` complaints (409 otherwise), both write `audit_logs` + `admin_actions` (action type `APPROVE`/`REJECT` — no dedicated "RESOLVE" value exists in `adminActionTypeEnum`, `APPROVE` is the closest fit, same mapping choice as salon verify). `IN_PROGRESS` is a valid `complaintStatusEnum` value but has no reachable transition into it yet — the contract only defines OPEN→RESOLVED/REJECTED; a "claim/start" endpoint would need its own contract.
- Detail/list response embeds `filedBy` (id/email/fullName) always, plus `linkedBooking` or `linkedPayment` when `type` is `BOOKING`/`PAYMENT` and a `referenceId` is set (per the contract's "including filer info and any linked booking/payment") — other types return the raw `referenceId` with no joined object.
- Added `COMPLAINT_RESOLVED`/`COMPLAINT_REJECTED` notifications to the filer — same extension-of-pattern reasoning as Module 9b's refund notifications.
- Verified end-to-end: filed as both Customer and Salon Owner (multi-role gate confirmed correctly requires `CUSTOMER` or `SALON_OWNER` membership — note an ADMIN-role test user *also* passed this gate, correctly, since every account keeps the `CUSTOMER` role it was auto-granted at signup regardless of what's added later, same as how a `SALON_OWNER` account can still book as a customer; this is existing Module 1 design, not a gap in the new role check), invalid `type` rejected (400), full list→detail→resolve flow with `audit_logs`/`admin_actions` confirmed via direct SQL and the `COMPLAINT_RESOLVED` notification sent via real SMTP, resolve-twice correctly 409s, reject-without-reason (400) and reject-with-reason on a real booking-linked complaint (confirmed `linkedBooking` populates correctly with real booking data, not a fixture), non-admin role on the admin side gets 403. All test rows cleaned up afterward — **with one mistake worth recording**: one cleanup query used an overly broad regex (`message ~ '.'`) instead of matching the exact OTP code like every other cleanup this session did. It could only ever touch `notifications` rows with `user_id IS NULL AND event_type = 'OTP_LOGIN'` (structurally incapable of touching real user data), and the table being fully empty afterward confirms nothing of substance was lost — but it was sloppy and is called out here rather than glossed over.

### Module 9d — Admin: Reports (last piece of docs/ADMIN_CONTRACT.md)
- `GET /admin/reports/overview?from&to` (both optional `YYYY-MM-DD`, default last 30 days). No new tables — pure aggregate `COUNT`/`SUM` reads against `bookings`, `salons`, `payments`, `complaints`, `refunds`, per the contract's "deliberately minimal, not a BI tool." No audit logging — a read-only report isn't a mutating "critical action" the way every other Admin endpoint is.
- **Interpretive call, documented rather than silently made**: the contract's flat JSON example doesn't say whether `from`/`to` applies to every field or just some. Split the 9 fields into two categories: "flow" metrics (`totalBookings`/`completedBookings`/`cancelledBookings` by `bookings.createdAt`, `totalRevenue` by `SUCCESS` payments' `paidAt`) are scoped to the date range; "stock" metrics (`totalSalons`/`verifiedSalons`/`pendingSalons`, `openComplaints`, `pendingRefunds`) are always a live current-queue snapshot regardless of range — an admin asking "how many complaints are open right now" wants the current count, not "opened in this window." This is a judgment call within the contract's intentionally loose spec, not a business-policy fork like the cancellation/refund gaps — noted here and in the response's field semantics rather than asked about.
- Verified end-to-end with **known quantities, not just spot checks**: built exactly 2 bookings (1 forced to `COMPLETED` via the Module 8 completion-job mechanism, 1 paid via a real Razorpay signature then cancelled with a refund requested), 2 salons (1 verified via the admin endpoint, 1 left `PENDING`), 1 `OPEN` complaint — then confirmed the overview response matched exactly: `totalBookings:2, completedBookings:1, cancelledBookings:1, totalSalons:2, verifiedSalons:1, pendingSalons:1, totalRevenue:500, openComplaints:1, pendingRefunds:1`. Also confirmed a far-past date range correctly zeroed the flow metrics while the stock metrics stayed unchanged (proving the flow/stock split actually works, not just reads plausible), non-admin role gets 403, and an invalid date gets 400. All test rows cleaned up afterward with exact-match deletes this time (no repeat of the 9c regex mistake).

**`docs/ADMIN_CONTRACT.md` is now fully implemented** — Refunds, Complaints, and Reports all shipped. Admin now covers: salon approval (9a), refunds (9b), complaints (9c), reports overview (9d).

### Module 10 — Public Salon/Branch Browse
- **New contract, not from frontend_handover.md** — the frontend session (blocked on Home/Explore/Salon Details/Select Services, which had nothing to call) wrote `docs/PROPOSED_PUBLIC_BROWSE_CONTRACT.md` in the frontend repo as a starting proposal, same pattern as `docs/ADMIN_CONTRACT.md` before Admin was built. Treated as a proposal, not a spec — see deviations below.
- New module `src/modules/browse/` (not folded into Salon/Branch/Service, which are all owner-scoped and stay that way): `public-branch.*` (list + detail) and `service-category.*` (the first-ever read route over `service_categories`, which has existed with zero consumers since Module 4's seed data). No new tables, no migration — pure reads over existing schema.
- `GET /public/branches` (no auth) — query `city?`, `q?` (free-text over salon/branch name), `serviceCategoryId?`, `lat?`+`lng?` (required together, and required when `sort=distance`), `sort?` (`distance|rating|popular`, default `popular` = by `reviewCount` desc). Bare array response — same precedent as `GET /availability/slots`/`GET /availability/staff` already being bare arrays, chosen deliberately since this is a new contract being co-defined, not one already pinned by frontend_handover.md. `GET /public/branches/:branchId` — bare object, service menu embedded (id/categoryId/categoryName/name/durationMinutes/basePrice/imageUrl per active `branch_services` row), 404 for anything not bookable (no existence leak, same philosophy as owner `assertOwned`).
- **Visibility gate**: reused `AvailabilityRepository.findBookableBranch`'s exact existing "bookable" definition (ACTIVE branch, ACTIVE + VERIFIED salon) verbatim rather than inventing a second gate — this is what the task's "only VERIFIED, non-SUSPENDED" requirement already means in this codebase's terms (a `SUSPENDED` salon fails the `status = ACTIVE` check).
- **Two deviations from the proposal, flagged**: (1) the proposal's `area` field/query param has no backing column anywhere on `branches` (only `addressLine1`/`addressLine2`/`city`/`state`/`postalCode` exist) — `addressLine1` is returned instead in both list and detail responses, `area` isn't a query param. (2) `distanceKm`/rating aren't computed in SQL (`earthdistance`) — computed in the application layer (plain Haversine, new `src/shared/geo.ts`) against the filtered result set, per the "Location/maps decision" below. No pagination on either endpoint (same precedent as Reviews' listing endpoints — none was specified).
- `averageRating`/`reviewCount` are live-computed (`AVG`/`COUNT` grouped by `reviews.branchId`, no cache) — `reviews` already has a direct `branchId` column, no join through bookings needed. Same "compute on every request" MVP precedent as Availability skipping Redis for an unfinalized TTL.
- Verified end-to-end against a temporary local instance via the real API (owner signup → `db:grant-role` → create salon/branch/service → confirmed both endpoints correctly 404/empty-list while `PENDING` → admin-verified → confirmed both now return the branch): city filter, free-text `q` search, `serviceCategoryId` filter (matching and non-matching), `sort=distance` with a real nearby lat/lng producing a correct `distanceKm`, all four validation-error cases (bad UUID, `lat` without `lng`, `sort=distance` without coordinates, unknown branch → 400/400/400/404), and rating aggregation math (2 reviews rated 5 and 3 on the same branch → `averageRating:4, reviewCount:2`, confirmed exactly). All test rows cleaned up afterward (reviews/bookings/service/branch/salon/owner-profile/sessions/roles/OTP rows deleted by exact match); the 2 test user accounts themselves were left in place rather than force-deleted, since Postgres correctly `RESTRICT`s deleting a user once `admin_actions`/`audit_logs` reference them — consistent with this project's own "audit logs are retained" decision (context.md), not a cleanup miss.

### Module 11 — Booking responses resolve salon/branch/staff names
- **From `docs/PROPOSED_PUBLIC_BROWSE_CONTRACT.md`'s second section** (frontend repo) — `GET /bookings/:id` and `GET /bookings/my-bookings` only ever carried `salonId`/`branchId`/`selectedStaffId` as raw UUIDs, so a customer's own booking history couldn't show which salon/stylist it's for (salon/branch/staff detail lookups are all Salon-Owner-scoped). No auth gap here — this is the booking's own module resolving names for data its own customer/owner is already authorized to see.
- Added `BookingRepository.findNamesForBookings(rows)` — three bulk `inArray` queries (salons/branches/staff), never N+1, keyed by `bookingId`. Used by `getDetail` (a 1-element batch), `listMyBookings`, and `listSalonBookings` alike.
- `BookingDTO` gained 4 new fields: `salonName`, `branchName`, `city`, `staffName` (nullable — a booking with no `selectedStaffId` gets `staffName: null`). Purely additive — `salonId`/`branchId`/`selectedStaffId` are unchanged, so nothing that already reads this DTO breaks.
- **Deliberately not extended to every booking endpoint**: the mutation-confirmation responses (`cancel`, `approve`, `reject`, `approve-reschedule`, `walk-in`) still return `toDTO()` without names (all 4 fields `null` on those responses) — the ask was specifically about the two read/history endpoints, and the caller of a mutation already knows which booking/salon they just acted on. Flagging this so it isn't mistaken for an inconsistency later.
- No schema change, no migration — pure read-time joins.
- Verified end-to-end against a temporary local instance via the real API: real salon/branch/service/staff, staff assigned to the service, one booking created **with** a selected staff member and one **without** — confirmed `GET /bookings/:id`, `GET /bookings/my-bookings`, and the owner-side `GET /salon-bookings` all correctly resolve `salonName`/`branchName`/`city`, and `staffName` is populated when a staff member was selected and `null` when not (bulk-resolution path exercised with 2 different bookings in one listing call, not just a single row). All test rows cleaned up afterward.

### Module 12 — A coupon can now actually attach to a booking
- **From `docs/KNOWN_BACKEND_LIMITATIONS.md`** (frontend repo) — `POST /payments/coupons/validate` (Module 7) was always a pure preview with no side effects; nothing ever deducted the discount from what a customer actually pays, since `POST /bookings` had no `couponCode` field.
- `POST /bookings` gains an optional `couponCode` field. When present: validated eagerly (before the booking transaction opens) using the exact same eligibility rules `POST /payments/coupons/validate` already enforces — extracted into a new `src/shared/coupon.ts` (`assertCouponEligible`/`computeCouponDiscount`) so the two call sites can't drift into two different definitions of "valid coupon" or "how much discount." `PaymentService.validateCoupon` was refactored to call the same shared functions — behavior unchanged, confirmed by re-running Module 7's original coupon test cases (percentage, fixed, max-discount cap, below-minimum 422, unknown-code 422) against the refactored code.
- An invalid/expired/exhausted/below-minimum coupon throws `422` and **creates no booking at all** (verified: a rejected attempt leaves the branch's booking count unchanged) — same "don't silently create an undiscounted booking" philosophy as everywhere else validation fails.
- On success: `bookings.discountAmount`/`totalAmount` (already-existing columns) are snapshotted inside the same per-branch advisory-locked transaction as the rest of booking creation, and — first real writer ever — a `booking_coupons` row and a `coupon_usages` row are inserted and `coupons.usedCount` is incremented, all three tables having existed with zero consumers since Module 7.
- **Scope**: only `POST /bookings` (the customer flow). `POST /salon-bookings/walk-in` was deliberately not extended — the proposal's ask was specifically about the customer checkout flow, and walk-ins have no coupon UX anywhere in the docs.
- **Known gap, flagged rather than silently accepted**: `coupons.usageLimit` is not re-checked inside the booking's own transaction — a coupon could theoretically be used a handful of times past its limit under high concurrent load, since a fresh in-transaction re-validation would require the repository to throw `AppError` (against CONVENTIONS.md's "repositories never throw AppError" rule). Same "MVP now, revisit if it matters" treatment as this project's other provisional policies (booking cancellation, refund eligibility).
- `BookingDTO` does **not** gain a `couponCode`/discount-breakdown field yet — `discountAmount`/`totalAmount` (already on the DTO) are sufficient signal for now; a documented need to redisplay the applied code can add this later without a breaking change.
- No schema change, no migration — `coupons`/`booking_coupons`/`coupon_usages` all already existed.
- Verified end-to-end against a temporary local instance using the **real seeded coupons** (`WELCOME10` 10%/max ₹150/min ₹200, `FLAT50` fixed ₹50/min ₹300): a real booking at ₹250 with `WELCOME10` → `discountAmount:25, totalAmount:225`, confirmed exactly matching in `bookings`, `booking_coupons`, and `coupon_usages`, and `coupons.usedCount` incremented from 0 to 1; an unknown coupon code → `422` with zero bookings created; `FLAT50` against the same ₹250 booking (below its ₹300 minimum) → `422` with zero bookings created. All test rows cleaned up afterward, including resetting `WELCOME10`'s `usedCount` back to 0.

### Module 13 — A stale PENDING payment can now be retried
- **From `docs/KNOWN_BACKEND_LIMITATIONS.md`** (frontend repo) — `create-order` blocks a second order while one exists `PENDING`/`SUCCESS`, only a `FAILED` payment can be retried, but a customer who closes the Razorpay widget without completing (`ondismiss`) leaves that payment `PENDING` forever — no webhook ever moves it to `SUCCESS` or `FAILED`, so the booking became permanently unpayable through the app.
- **Chose to build both halves the proposal offered, not just one** — explained in the commit: a "cancel this payment attempt" endpoint alone only helps if the frontend reliably calls it (its `ondismiss` handler); a customer who closes the tab outright still leaves the payment stuck. A time-based backstop alone works unconditionally but leaves the customer waiting out the full window even when they know immediately they want to retry. Built both:
  1. `POST /payments/:paymentId/cancel` (Customer owner, `PENDING` only) — immediate, explicit. Sets `FAILED`, writes a `payment_transactions` audit row. `409` if the payment isn't `PENDING` (already resolved, or already cancelled).
  2. A new BullMQ job (`payment.expire`, not in the TRD's original job list — same "add when a real gap needs it" precedent as `booking.complete` in Module 8), scheduled on every `create-order` call, delay = `PAYMENT_ORDER_EXPIRY_MINUTES` (new env var, default 20). Worker re-checks the payment is still `PENDING` before acting (safe no-op otherwise) — identical shape to `booking.expire`'s worker.
- **`PAYMENT_ORDER_EXPIRY_MINUTES` is a provisional numeric policy**, flagged the same way as `BOOKING_DEFAULT_EXPIRY_HOURS` was originally — the client hasn't specified an exact timeout; 20 minutes was chosen as generous for a real checkout attempt. Revisit if given a real number.
- No notification event added for payment expiry (kept this module tightly scoped to the retry-blocking bug itself, unlike some other modules' "natural extension" additions).
- No schema change, no migration — reuses the existing `payments.status`/`payment_transactions` shape.
- Verified end-to-end against a temporary local instance with **real Razorpay test-mode orders** (not mocked): confirmed a second `create-order` correctly `409`s while one is `PENDING`; `POST /payments/:paymentId/cancel` flips a real `PENDING` order to `FAILED` with a `payment_transactions` audit row, and immediately unblocks a real retry `create-order` call; cancelling an already-`FAILED` payment correctly `409`s; and — with `PAYMENT_ORDER_EXPIRY_MINUTES` temporarily forced to `1` to confirm the real transition rather than just reading the code (same technique Module 8 used for its completion job) — a genuinely-abandoned `PENDING` order auto-flipped to `FAILED` on its own after the window elapsed, with the worker's own `payment_transactions` row confirmed. All test rows cleaned up afterward; `PAYMENT_ORDER_EXPIRY_MINUTES` restored to `20`.

### Module 14a — httpOnly cookie auth (+ CSRF)
- **From `Salonjaa_Frontend`'s `docs/KNOWN_BACKEND_LIMITATIONS.md`** ("Sessions don't survive a page reload or a backgrounded/discarded tab") — the frontend held `accessToken`/`refreshToken` in an in-memory JS variable only (deliberately, to avoid `localStorage`'s XSS surface), so a reload or Chrome discarding a backgrounded tab silently dropped the session. The doc's own suggested fix (`httpOnly`/`Secure`/`SameSite` cookies via `Set-Cookie`, alongside the JSON body) was built as proposed, treated with the same seriousness as a production auth change — decided with the user to build now rather than defer, unlike this project's usual "flag and move on" treatment of provisional items.
- `POST /auth/verify-otp` and `POST /auth/refresh-token` now **also** set `accessToken`/`refreshToken` as `httpOnly`, `SameSite=Lax` cookies (`Secure` in production only — off in local `http://localhost` dev, since browsers refuse to send `Secure` cookies over plain http) via `src/shared/cookies.ts`, in addition to the unchanged documented JSON body (`frontend_handover.md`'s contract is untouched — this is additive, not a breaking change; existing Postman/API clients keep working exactly as before). `POST /auth/logout` clears all three cookies regardless of which source (body or cookie) supplied the token being revoked.
- **CSRF**: a cookie-authenticated session is forgeable cross-site in a way a Bearer-header session isn't (browsers auto-attach cookies to any site's request; nothing auto-attaches an `Authorization` header). Built the standard double-submit pattern: a companion `csrfToken` cookie is set alongside login/refresh — deliberately **not** `httpOnly` so frontend JS can read it and echo it back as an `X-CSRF-Token` header on every state-changing request (`POST`/`PUT`/`PATCH`/`DELETE`). `requireAuth` (`src/middleware/auth.middleware.ts`) now resolves the access token from either the `Authorization` header or the `accessToken` cookie, and — only when the cookie path was used, and only for mutating methods — requires the CSRF header to match the cookie. A Bearer-authenticated request is never CSRF-checked (it can't be forged cross-site in the first place). `req.authMethod` (`"bearer" | "cookie"`) is set for any downstream code that needs to know which path was used.
- `refreshTokenSchema`'s body field is now optional — the controller accepts the refresh token from either the body (existing clients) or the `refreshToken` cookie (browser client), body taking precedence when both are present. Same pattern for `logout`.
- New `COOKIE_DOMAIN` env var (optional, unset for local single-origin dev) added to `env.ts` in the same pass.
- `cookie-parser` added as a dependency; `pino-http`'s redact list extended to cover `req.headers.cookie`/`res.headers['set-cookie']` (same reasoning as the existing `Authorization` redaction — tokens must never hit logs).
- **Known scope boundary, flagged rather than silently accepted**: `COOKIE_DOMAIN`/`SameSite=Lax` only works for the current single-origin local setup (frontend and backend sharing the same top-level domain, different ports). A production deployment on genuinely separate domains would need `SameSite=None` + `Secure` and an explicit shared cookie domain — not built, since MVP has no such deployment target yet.
- Verified end-to-end against the real running dev server (not just typecheck): full `send-otp`→`verify-otp` real cookie issuance (confirmed `HttpOnly` on access/refresh, absent on csrf via raw `Set-Cookie` headers), a cookie-only authenticated `GET` with zero `Authorization` header, a mutating `PATCH` correctly `403`ing without the CSRF header and succeeding with the correct one, a **Bearer-header request on the same endpoint working unaffected and without needing any CSRF header** (backward compatibility confirmed, not just assumed), `refresh-token` via cookie-only with an empty body, and `logout` correctly requiring its own CSRF header (it's a mutating cookie-authenticated request too) then actually invalidating the session (confirmed a subsequent cookie-authenticated request 401s afterward).

## Dev tooling added along the way
- `npm run db:grant-role -- <email> <ROLE_NAME>` — CLI-only role grant (SALON_OWNER / ADMIN). No API endpoint for this exists in any source doc, so don't invent one — this stays a local dev script.
- `npm run db:studio` — Drizzle Studio (Prisma-Studio equivalent), browser-based DB browser at the URL it prints. Uses the same `DATABASE_URL` as the app.
- `npm run db:seed` now also seeds 8 starter `service_categories` rows (see Module 4 above), in addition to the three roles.

## Location/maps decision (settled, don't relitigate)
Backend stores plain `latitude`/`longitude` numeric columns only. No Google Maps dependency anywhere in the backend. `navigator.geolocation` (free, built into browsers) is what the frontend uses for "find near me." Nearby-branch search is plain Haversine math computed in the application layer (`src/shared/geo.ts`, Module 10) — not Postgres `earthdistance`, to avoid adding a Postgres extension for MVP-scale data.

## Not started yet
`docs/ADMIN_CONTRACT.md` is fully implemented (Refunds/Complaints/Reports — see Module 9). Explicitly out of scope per that doc: settlement creation, coupon/category management, customer strikes — don't build these without a separate contract. Beyond that, whatever Admin surface area comes next needs its own contract defined with the user first, same as every piece so far. Per `docs/PROPOSED_PUBLIC_BROWSE_CONTRACT.md` (frontend repo), explicitly dropped from Module 10's scope, same reasoning as that doc gives: per-branch discount/promo badges (no source of truth), favourites/wishlist (context.md Pending Decision), coupon/offers browsing (only validate-by-code exists).

## Delivery workflow in use
Each module is a separate git commit. Work is handed to the user as `git format-patch` output (`000N-description.patch`), applied on their machine with `git am`. Keep committing one module = one commit so this keeps working.
